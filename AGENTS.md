## Agent skills

### Issue 跟踪器

GitHub Issues（用 gh CLI）。见 `docs/agents/issue-tracker.md`。

### Triage 标签

默认 5 标签：needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix。见 `docs/agents/triage-labels.md`。

### Domain 领域文档

根目录 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
