# 电信客服 Agent

一个面向电信用户的客服系统:LLM 主导多轮对话与意图识别,通过 tools 调用业务能力(查询话费、办理套餐、报修等);查询类(只读)直接返回,办理类(写入/不可逆)走二次确认与工单;特定条件下转人工坐席。

## 领域语言

### 角色与系统

**客服 (Agent)**：
面向用户承担服务职责的角色统称,涵盖 LLM 程序与人工坐席两种形态;用户视角下二者同属「客服」。
_避免使用_: 客服代表 (CSR), 客服人员, 助理

**助理 (Assistant)**：
客服角色中由 LLM 驱动的程序形态;负责多轮对话、意图识别、调用 tools 完成查询与办理发起;不可逆操作需经二次确认或转工单,不自主执行写入。
_避免使用_: AI 客服, 机器人 (Bot), Agent(作为程序名时)

**坐席 (Human Agent)**：
客服角色中由人工承担的形态;接收助理转交的会话,处理超出助理能力或需人工判断的场景。
_避免使用_: 人工客服, 客服代表, 转接目标

**技能组 (Skill Group)**：
按业务线划分的坐席集合(如套餐业务组、故障报修组、投诉处理组);`Handoff` 时按 `Ticket` 类型或用户意图路由到对应组,一个坐席可属多组。
_避免使用_: 坐席分组, 队列, 业务组

### 身份与主体

**用户 (User)**：
与客服系统交互的任意一方;广义概念,包含已认证与未认证者。
_避免使用_: 访问者, 使用者

**访客 (Visitor)**：
未通过身份认证的 `User`;只能做通用咨询(套餐介绍、覆盖范围、营业厅查询),不能查询或办理任何与号码绑定的业务。
_避免使用_: 游客, 匿名用户

**客户 (Customer)**：
已通过身份认证、与电信账户绑定的 `User`;拥有号码、套餐与合约等订阅关系。
_避免使用_: 订户 (Subscriber), 机主, 账户持有人

**服务密码 (Service Password)**：
客户办理业务时用于身份认证的密码;与号码绑定,是 `Visitor` 升格为 `Customer` 的认证凭证。
_避免使用_: 登录密码, 账户密码

### 业务规则

**一号一户**：一个 `Customer` 名下仅绑定一个号码;不存在一机主多号场景。

### 会话

**会话 (Conversation)**：
一个用户从进入到结束的完整交互序列;绑定一个 `User`(认证后绑定 `Customer`),可跨越多个 `Session`。
_避免使用_: 对话, 工单, 会话流

**会话片段 (Session)**：
一段连续活跃的交互;超时后断开,重新交互开启新 Session,归属同一 `Conversation`。
_避免使用_: 连接 (Connection), 上下文 (Context)

**转接 (Handoff)**：
助理将会话移交给坐席的过程;触发后坐席主导,助理退至后台协助(起草回复、检索知识、查信息),不再直接面向用户;坐席可显式将会话转回助理。会话历史保留并传递给坐席,不重置。
_避免使用_: 转人工, 转接坐席(作为流程名), 升级 (Escalation)

**工单 (Ticket)**：
需要后续跟踪的业务事项(办理、投诉、报修等);可由助理或坐席创建,一个 `Conversation` 可关联多个 `Ticket`。
_避免使用_: 任务 (Task), 订单 (Order), 工作单

**消息 (Message)**：
`Conversation` 内的一条交互记录;按来源分四类:
- **用户消息 (User Message)** — 来自 `User`(或认证后的 `Customer`)
- **助理消息 (Assistant Message)** — 助理生成的回复
- **坐席消息 (Agent Message)** — `Handed-off` 状态下坐席的回复;用户视角仍是「客服」
- **系统消息 (System Message)** — 系统动作的提示(如「会话已转接」「坐席已上线」),无角色主体

助理调用 tools 的内部记录不属于 `Message`,不入对话流,仅入审计日志。

**通知 (Notification)**：
`Ticket` 状态变化时推送给用户的站内消息(对话内推送);v1 仅站内渠道,预留短信/推送扩展位。
- 办理类 Ticket:生效/失败时通知
- 工单类 Ticket:派单/关闭时通知

**审计日志 (Audit Log)**：
电信场景合规留痕,以下关键操作必须记录:服务密码认证(成功/失败)、查询类调用、办理类发起/二次确认/入队/执行/生效或失败、`Handoff` 发起与结束(含转出转入方与时间)、坐席操作(查看客户资料、修改 `Ticket`)、用户敏感数据访问(话费、合约、号码)。

### 会话状态机

`Conversation` 的状态:

- **未认证 (Unauthenticated)** — `Visitor` 进入,尚未通过服务密码认证;只能做通用咨询。
- **已认证 (Authenticated)** — 通过服务密码,升格为 `Customer`;可查询与发起办理。
- **办理中 (In-Progress)** — 正在等待某项办理业务的二次确认;一旦用户确认并创建 `Ticket` 入队,会话状态回到 `Authenticated`,用户可继续发起下一项办理。该状态仅覆盖「等待二次确认」阶段,不覆盖整个 `Ticket` 生命周期。
- **已转人工 (Handed-off)** — 助理已 `Handoff`,坐席主导,助理后台协同。
- **已结束 (Closed)** — 会话终止,后续交互开启新 `Conversation`。

### 业务能力

助理可执行的业务分四类:

- **查询类 (Inquiry)** — 只读操作,`Customer` 认证后可直接调用;返回当前账户状态与历史信息。
_避免使用_: 读取 (Read), 查看类

- **办理类 (Transaction)** — 写入或不可逆操作,如套餐变更、增值业务订退、停机保号、充值缴费;助理发起后必须经二次确认,且一律创建 `Ticket` 进入待执行队列,不直接生效。
_避免使用_: 操作类 (Operation), 业务变更

- **通用咨询类 (General Info)** — 无需认证,`Visitor` 即可查询;面向公开信息(套餐介绍、网络覆盖、营业厅地址)。
_避免使用_: 公共查询, FAQ

- **工单类 (Ticketing)** — 受理需后续跟踪的业务事项(故障报修、投诉);`Visitor` 可创建,涉及本人号码事项需认证。

### 办理规则

**二次确认 (Second-confirmation)**：办理类业务执行前,助理必须返回结构化确认提示(包含业务影响:套餐对比、生效时间、合约影响、费用变化),用户显式确认后才创建 `Ticket`;未确认不进入待执行队列。

**办理入队**：所有办理类业务一律经二次确认后创建 `Ticket`,进入待执行队列;无任何办理业务绕过 `Ticket` 直接生效。

**办理执行复核 (Transaction Re-auth)**：办理类 `Ticket` 从「待执行」进入「执行中」前,必须要求用户再次输入 `Service Password` 验证通过方可执行;作为单因素认证的补偿控制,防止会话被劫持后办理不可逆业务。

### 工单状态机

`Ticket` 统一模型(含 ID、所属 `Conversation`、创建者、类型、内容、创建时间、状态、关联 `Customer`),按类型走不同状态机:

- 关联 `Customer` 字段允许为 null(`Visitor` 创建工单时),仅记录联系方式(姓名+电话);认证后或坐席补全时回填。
- 一个 `Conversation` 可并存多个 `Ticket`。

- **办理类 Ticket**:`待执行 (Pending)` → `执行中 (Processing)` → `已生效 (Effective)` / `已失败 (Failed)` / `已取消 (Cancelled)`
- **工单类 Ticket**:`待派单 (Pending)` → `已派单 (Dispatched)` → `处理中 (In-Progress)` → `待确认 (Awaiting-confirmation)` → `已关闭 (Closed)` / `已取消 (Cancelled)`

### 服务时间与离线兜底

**助理在线时间**：7×24 全时在线,可处理查询类、咨询类与办理入队;无坐席时不阻断会话。

**坐席服务时间**：1:00-23:00;非服务时间 `Handoff` 进入待接入队列,次日服务时间开始坐席接入。

**离线兜底**：当非服务时间或服务时间内所有坐席忙线超阈值时,助理告知用户「当前无可用坐席,已为您创建 `Ticket`,坐席将在服务时间联系您」,创建 `Ticket`(类型:回呼请求)派单到对应 `Skill Group`;助理继续处理可处理的业务,不强制结束 `Conversation`。

### 转接触发

助理在以下条件**自动触发** `Handoff`(无需用户请求):

1. **超出能力范围** — 用户请求 v1 范围外的业务(如挂失、销户、查账单明细)。
2. **办理失败** — `Ticket` 入队失败或后端业务系统返回错误,助理无法自动恢复。
3. **明确请求** — 用户显式说「转人工」「找客服」。
4. **负面情绪** — 检测到强烈负面情绪(关键词匹配脏话/负面词 + 连续 2 轮触发),避免激化。
5. **同一意图循环** — 同一意图连续 3 轮未能完成(用户反复纠正助理理解)。
6. **合规风险** — 涉及投诉金额、违约金争议、合约条款争议,需人工判断。

### 知识来源

助理回答用户咨询依赖三类来源:

- **结构化数据 (DB)** — 套餐目录、营业厅列表、资费表;助理通过查询类业务能力查询。
- **非结构化文档 (RAG)** — 政策文档、业务规则、操作手册;助理通过向量检索回答(如「合约期内能否销户」「5G 套餐能否转 4G」),避免编造政策引发合规风险。
- **静态话术 (Prompt)** — 问候语、转接话术、合规提示;直接写入助理 system prompt。

v1 全量启用三类来源,含 RAG。

### v1 业务范围

助理 v1 覆盖:

- 查询类:话费余额、当前套餐详情、通话/流量使用量、合约到期时间、已订购增值业务
- 办理类:套餐变更、增值业务订购/退订、停机保号、充值缴费
- 通用咨询类:套餐介绍与对比、网络覆盖查询、营业厅地址查询
- 工单类:故障报修

v1 明确排除:账单明细查询、历史账单、充值记录、挂失/解挂、注销号码、投诉受理(走标准 Ticket 流程,故障报修已覆盖)。

