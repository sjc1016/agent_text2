# DESIGN.md 模板

`/grill-with-docs` 在品牌视觉规范确认后，惰性创建 `docs/design/DESIGN.md`。

## 产出目标

文档须**完整到可还原品牌板**——即一张可视化总览，包含：


| 品牌板区域 | 文档须给出的内容                                                   |
| ----- | ---------------------------------------------------------- |
| 色板    | 主色 / 辅色 / 第三色 / 中性色 四组 + 每组 8–10 阶色阶（附 HEX）                |
| 语义色   | error / success / warning / info 各 Token + HEX（不含业务状态）        |
| 叠色    | §4 叠色对照表：复用 ≥2 次的叠色须固化 Token + 预计算 HEX                     |
| 字体    | 标题 / 正文 / 标签 三档字体族、样例与使用场景；受限运行时须含实现约束与 fallback 栈         |
| 组件样张  | 见下方「品牌板组件表」——通用 UI 原语逐项覆盖（不含功能页面规格）              |


### 品牌板组件表（通用 UI 原语，§5 须逐项覆盖）

| #   | 原语        | 文档须给出的内容                                               |
| --- | --------- | ------------------------------------------------------ |
| 1   | 按钮变体      | 主 / 次 / 描边 / 文字 / **反色**；各含背景 / 文字 Token、圆角、悬停 / 按下态   |
| 2   | 输入与表单     | 默认 / 聚焦 / 错误态；边框或外发光规则；错误态引用 §2 `semantic-error` Token |
| 3   | 卡片容器      | 圆角、内边距、阴影、背景 Token；**不写**业务内容排列                         |
| 4   | 列表行       | 行高、行间距、分隔方式、选中 / 高亮态；**不写**业务列字段                      |
| 5   | 导航        | 顶栏 / 侧栏 / 底栏 Tab 等主形态与选中态                              |
| 6   | 搜索框       | 输入框与下拉结果的视觉 pattern（无全局搜索须注明跳过）；**不写**业务占位符           |
| 7   | 状态徽章      | 组件形态（padding、圆角、字号）；附 1 条通用样例；**不写**业务状态名               |
| 8   | 图标按钮      | 尺寸、描边粗细、默认 / 悬停 / **选中态** Token（Tab 图标选中态在此定义）         |
| 9   | 空状态       | 插画或图标尺寸、文案字号与颜色、可选 CTA 按钮变体（通用 pattern）               |
| 10  | Loading   | spinner 或骨架屏形态与尺寸（不适用须在文档注明跳过）                         |


正文用叙事性设计说明，含明确规则、Token 名与 HEX、组件行为描述。文首注明 `ui-mode`（`spec-driven` 规范驱动 UI 或 `mockup-driven` 稿驱动 UI）。

**叠色规则：** 复用 ≥2 次的叠色须在 §4 对照表固化；§5 引用 Token 名，禁止裸写 `primary @ N%`。单次不复用的叠色可直接写 HEX。mockup-driven 下页面视觉以 PNG 为准，但叠色对照表仍须可独立画板。

**语义色规则：** error / success / warning / info 须在 §2 定义。§5 输入错误态引用 `semantic-error`；§5 状态徽章只写组件形态与通用样例，不写业务状态名或状态色映射。

**§5 范围规则：** 只写通用 UI 原语（atom/molecule）。禁止：页面名、业务字段布局、功能专属复合组件、领域术语、业务状态名。功能页面：mockup-driven → PNG 稿；spec-driven → 实现规划阶段，不写入 DESIGN.md。

**字体实现规则：** 受限运行时（小程序 / RN / Flutter / 原生 APP）须在 §3 写明加载策略与 fallback 栈。mockup-driven 下 PNG 管视觉，§3 管 Token 与实现边界；稿面字体与实现字体不一致时须分别列出。

## 章节映射


| 拷问轮次           | 正文章节                            |
| -------------- | ------------------------------- |
| 1 创意北极星        | `## 1. 概览与创意北极星`                |
| 2a 品牌四色板       | `## 2. 色彩与表面架构`（色板 / 表面 / 布局规则） |
| 2b 语义色         | `## 2. 色彩与表面架构`（语义色）    |
| 3 字体（含实现约束）    | `## 3. 字体`                      |
| 4 层级与深度（含叠色固化） | `## 4. 层级与深度`                   |
| 5 通用 UI 原语     | `## 5. 组件`                      |
| 6 宜忌           | `## 6. 宜忌`                      |




## 模板

```markdown
# 设计系统文档：[产品名]

> UI 模式：`spec-driven`（规范驱动 UI）| `mockup-driven`（稿驱动 UI）

## 1. 概览与创意北极星

### 创意北极星：「[创意代号]」
[2–3 段：设计哲学、产品气质、与同类产品的差异化。说明如何打破「模板感」——如不对称、留白作结构、层叠与对比等。]

---

## 2. 色彩与表面架构

[一段话说明色板意图：主色代表什么、辅色代表什么、整体明暗基调。]

### 色板（品牌板须可视化）

| 角色 | 主色 HEX | 色阶（浅→深，8–10 阶，附 HEX） |
|------|---------|-------------------------------|
| 主色 | #______ | #___ … #___ |
| 辅色 | #______ | #___ … #___ |
| 第三色 | #______ | #___ … #___ |
| 中性色 | #______ | #___ … #___ |

### 语义色（品牌板须可视化）

error / success / warning / info 各给 Token + HEX；注明从品牌色衍生还是独立色。不适用的语义色须注明跳过。

| 角色 | Token | HEX | 来源 |
|------|-------|-----|------|
| 错误 | `semantic-error` | #______ | [如主色系衍生 / 独立色] |
| 成功 | `semantic-success` | #______ | [如辅色衍生 / 独立色] |
| 警告 | `semantic-warning` | #______ | […] |
| 信息 | `semantic-info` | #______ | […] |

### [布局边界规则名，如「无描边规则」]
**明确指令：** [禁止什么、用什么替代——如禁止 1px 实线边框，改用背景色阶或色调过渡分隔。]

### 表面层级与嵌套
[表面层级：底层 → 一级区块 → 卡片/模块 → 浮层/弹窗，每层附 Token 名与 HEX。]

### [特殊质感规则名，如「玻璃与渐变规则」]（若适用）
[毛玻璃、渐变、纹理等签名处理及其使用场景。]

---

## 3. 字体：[副标题，如「编辑感版式」]

[说明标题字体与正文字体的配对理由。]

### 字号阶梯（品牌板须可视化）

| 角色 | 字体族 | 用途 | 样例层级 |
|------|--------|------|---------|
| 标题 | [如 Space Grotesk] | 大标题、关键数据 | 超大标题 … 小标题 |
| 正文 | [如 Inter] | 正文、导航 | 大标题 … 小正文 |
| 标签 | [如 Inter] | 标签、辅助说明 | 中标签 … 小标签 |

### 信息层级
[标题与标签的对比策略、留白与字号搭配原则。]

### 字体实现约束（受限运行时须填写）

触及时须说明：自定义字体加载策略、fallback 栈、稿面字体 vs 实现字体。纯 Web 且无自定义字体限制时可注明「不适用，跳过」。

| 触点 | 稿面字体 | 实现字体 / fallback 栈 | 加载策略 |
|------|---------|----------------------|---------|
| [如微信小程序] | [如思源圆体] | [如 `-apple-system, 'PingFang SC', sans-serif`] | [如 wx.loadFontFace / 系统字体降级 / 以 PNG 为准] |
| [如 Web 管理端] | […] | […] | […] |

---

## 4. 层级与深度

[层级通过色调叠层而非线框建立。]

*   **叠层原则：** [卡片与背景的叠层关系及对应 Token。]
*   **环境阴影：** [浮动元素阴影规格：模糊、透明度、着色；避免纯黑阴影等。]
*   **幽灵描边兜底：** [若必须描边时的透明度与 Token 规则。]

### 叠色对照表（品牌板须可视化）

凡在 §5 组件/状态里 **复现 ≥2 次** 的叠色须在此固化；§5 引用 Token 名，禁止仅写 `@ N%` 简写。

| Token | 基准色 | 不透明度 | 预计算 HEX | 用途 |
|-------|--------|---------|-----------|------|
| `primary-tint-10` | primary-500 | 10% | #______ | [如选中态背景] |
| `primary-tint-8` | primary-500 | 8% | #______ | [如幽灵描边 / 悬停底] |
| … | … | … | … | … |

---

## 5. 组件

[各组件须有足够细节，使品牌板可画出样张。**只写通用 UI 原语**，不写页面名、业务字段布局、功能专属复合组件、领域术语、业务状态名。复用叠色引用 §4 Token 名；单次叠色直接写 HEX。]

### 按钮与交互（品牌板须可视化）
*   **主按钮：** [背景 Token、文字 Token、圆角、悬停 / 按下态]
*   **次按钮：** […]
*   **描边按钮：** […]
*   **文字按钮：** […]
*   **反色按钮：** [深色底或深色面上的浅色按钮；背景 / 文字 Token、悬停态]

### 输入与表单
*   [默认态、聚焦态、错误态；背景与边框 / 外发光规则；错误态边框 / 文案引用 `semantic-error` Token]

### 卡片容器
*   [圆角、内边距、阴影、背景 Token；不写业务内容排列]

### 列表行
*   [行高、行间距、分隔方式、选中 / 高亮态视觉规则；不写业务列字段]

### 导航
*   [顶栏 / 侧栏 / 底栏 Tab 等主形态；选中态 Token]

### 搜索框
*   [输入框与下拉结果的视觉 pattern；或注明「无全局搜索，跳过」；不写业务占位符]

### 状态徽章
*   [组件形态：padding、圆角、字号；附 1 条通用样例，如背景 `semantic-error @ 15%`、文字 `semantic-error`；不写业务状态名]

### 图标按钮
*   [尺寸、描边粗细；默认 / 悬停 / 选中态 Token。底栏 Tab 图标选中态在此定义，与导航节一致]

### 空状态
*   [插画或图标尺寸、文案字号与颜色、可选 CTA 按钮变体；通用 pattern，不写具体业务文案]

### Loading（若适用）
*   [spinner 或骨架屏形态与尺寸；或注明「不适用，跳过」]

---

## 6. 宜忌

### 应当：
*   **应当** […]
*   **应当** […]

### 禁止：
*   **禁止** […]
*   **禁止** […]
```

